The python code in this repository are used to access AWS instance details.
         Convert the volume types
         Move AMis to S3 
         Read the data from google sheets
         Fetch EBS volume Details.
         Update the Instance owner tags.
Create IAM user for AWS CLI access. Create a profile in AWS CLI for the IAM user.
